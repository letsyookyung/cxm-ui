// grid 페이지 설정 초기값
export const pageOptionInit = Object.freeze({
    pageNo: 0,
    pageSize: 20,
    sortField: "inpDthms",
    sortDirection: "DESC",
  });
  
  // grid 페이지 정보 초기값
  export const pageTotalInit = Object.freeze({
    totalPages: 1,
    totalElements: 0,
    numberOfElements: 0,
    empty: false,
  });
  